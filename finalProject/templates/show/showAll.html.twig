{% extends 'base.html.twig' %}
{% block title %}
	{{title}}
{% endblock %}

{% block body %}
	<div class="container">
		{{ parent() }}
		<div class="afterHeader">
			<h3>
				{{title}}</h3>
		<a href={{ path('add_employee') }}>
			<button class="buttons">Add employee</button>
		</div>
		</a>
		<div class="showInfo">
			{% for employee in employees %}
				{% include './show/template_showEmployee.html.twig' %}
			{% endfor %}
		</div>
	</div>
{% endblock %}
